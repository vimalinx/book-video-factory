# Project AI evidence ledger

Managed by the `ai-experiment-ledger` Skill and `ai-ledger` command.
Manifests and the append-only event ledger are project records. Native-session indexes, raw logs, and private notes are local-private and must be included in private backups.
